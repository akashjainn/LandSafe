export async function register() {
  // Runs once per server start; place Sentry.setTag/Context here if needed
  // Example:
  // const { hostname } = require('os');
  // Sentry.setTag('host', hostname());
}
