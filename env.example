# GA4 (client-safe)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry
NEXT_PUBLIC_SENTRY_DSN=https://publicKey.ingest.sentry.io/projectId
SENTRY_DSN=https://privateKey@ingest.sentry.io/projectId

# (Optional) For source map uploads in CI
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=landsafe
# SENTRY_PROJECT=landsafe

# Database (example — adjust to your Neon / Postgres connection)
# DATABASE_URL=postgresql://user:password@host/db?sslmode=require
